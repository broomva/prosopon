# Rust build artifacts
/target/
**/*.rs.bk
Cargo.lock

# Node / Bun / Turbo (the repo is also a Turborepo — see ./package.json)
node_modules/
.turbo/
packages/*/dist/
packages/*/.turbo/
packages/*/*.tsbuildinfo

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Local env
.env
.env.local

# Derived schema outputs (regenerate with `prosopon schema`)
/schemas/*.generated.json

# Coverage / profiling
*.profraw
*.profdata
tarpaulin-report.html

# macOS
.AppleDouble
.LSOverride
